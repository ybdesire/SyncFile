v1 API

http://localhost:8000/v1/getAuthID?fmt=json&username=email@address&password=password

1. model(DB) class for registration. Should modify class userInfo.

	Class UserInfo
		userName
		password
		email
		authid
		lastAuthTime

2. logic

if user(userName & password) in DB:
then
{authid:'xxooxoxooxxoxo', error_code:1010, msg:'auth id get ok'}
(if authid null, generate new one; if authid not null, and lastAuthTime not exceed 18h, return the exists authid & update lastAuthTime; if authid not null, and lastAuthTime exceed 18h, update authid & lastAuthTime )
else
{authid:'', error_code:1011, msg:'user is not registed or user name/password in correct'}
